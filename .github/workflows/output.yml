name: Output Gihub Information
on: workflow_dispatch
jobs:
    output:
        runs-on: ubuntu-latest
        steps:
            - name: Output Github Context
              run: echo "${{ toJson(github) }}"